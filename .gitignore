node_modules/
dist/
*.tsbuildinfo
.DS_Store

# Real credentials never enter the repo
.env
.env.*
!.env.example

# test coverage output
coverage/

# Private maintainer notes — never committed (kept locally, outside the public repo)
RISKS.md

# Live-proof harness (ring 3) — a REAL daemon with a funded wallet.
# Never commit the real values; .env is gitignored.
VERUS_RPC_URL=http://127.0.0.1:18843
VERUS_RPC_USER=
VERUS_RPC_PASS=
# Gates (opt-in per run):
#   SDK_PUBLIC_DECODE=1   ring 2: decoderawtransaction against api.verustest.net
#   SDK_ALLOW_SPEND=1     ring 3: funded broadcast (spends testnet dust)
